<Window x:ConnectionId='1'
    x:Class="AkariOS.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AkariOS.App">

    <Grid x:ConnectionId='2' x:Name="RootElement" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom title bar (drag region) -->
        <Grid x:ConnectionId='3' x:Name="AppTitleBar" Height="40" Background="Transparent">

            <StackPanel
                Orientation="Horizontal"
                Spacing="12"
                Margin="16,0,0,0"
                VerticalAlignment="Center">
                <Image x:ConnectionId='10'
                                                   
                    Width="16"
                    Height="16"
                    Stretch="Uniform" />
                <TextBlock x:ConnectionId='11'
                    x:Name="TitleText"
                                            
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>
        </Grid>

        <!-- Shell content -->
        <NavigationView x:ConnectionId='4'
            x:Name="NavView"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            IsPaneToggleButtonVisible="True"
            IsTitleBarAutoPaddingEnabled="False"
            PaneDisplayMode="Left"
            OpenPaneLength="232"
                                                    
                                               
                                                           >

            <NavigationView.MenuItemTemplate>
                <DataTemplate                                  >
                    <NavigationViewItem x:ConnectionId='6'                                                 >
                        <NavigationViewItem.Icon>
                            <FontIcon x:ConnectionId='7'                        />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                </DataTemplate>
            </NavigationView.MenuItemTemplate>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Frame x:ConnectionId='8' x:Name="ContentFrame" />

                <InfoBar x:ConnectionId='9'
                    Grid.Row="1"
                    x:Name="ShellInfoBar"
                                                  
                                                      
                                                        
                                                            
                                                                  />
            </Grid>
        </NavigationView>
    </Grid>
</Window>

