<local:WizardStepPage
    x:Class="AkariOS.App.Views.SystemCheckPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AkariOS.App.Views">

    <ScrollViewer Padding="32,24">
        <StackPanel Spacing="14" MaxWidth="720" HorizontalAlignment="Left">
            <TextBlock Text="System Check" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Verifying your system against the playbook requirements."
                       Opacity="0.75" TextWrapping="Wrap" />

            <InfoBar IsOpen="True" Severity="Informational" IsClosable="False"
                     Title="Checks land in Slice 2"
                     Message="Build number, internet, antivirus and power-source checks are wired next." />

            <!-- Placeholder cards; Slice 2 fills real values + requirement states -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="This system" Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock x:Name="SpecText" Opacity="0.85" FontSize="13" TextWrapping="Wrap"
                               Text="Gathering…" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</local:WizardStepPage>