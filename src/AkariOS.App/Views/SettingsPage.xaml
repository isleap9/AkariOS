<Page
    x:Class="AkariOS.App.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="32" Spacing="16">

            <TextBlock
                Text="{x:Bind Strings.Get('Settings.Title'), Mode=OneWay}"
                Style="{StaticResource TitleTextBlockStyle}" />

            <!-- Appearance -->
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="16"
                MaxWidth="480">
                <StackPanel Spacing="12">
                    <TextBlock Text="{x:Bind Strings.Get('Settings.Appearance'), Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}" />

                    <ComboBox
                        Header="{x:Bind Strings.Get('Settings.Theme'), Mode=OneWay}"
                        ItemsSource="{Binding Themes}"
                        SelectedItem="{Binding SelectedTheme, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource EnumToStringConverter}}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <ComboBox
                        Header="{x:Bind Strings.Get('Settings.Language'), Mode=OneWay}"
                        Width="220"
                        HorizontalAlignment="Left"
                        ItemsSource="{Binding Cultures}"
                        DisplayMemberPath="DisplayName"
                        SelectedItem="{Binding SelectedCulture, Mode=TwoWay}" />
                </StackPanel>
            </Border>

            <!-- About -->
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="16"
                MaxWidth="480">
                <StackPanel Spacing="8">
                    <TextBlock Text="{x:Bind Strings.Get('Settings.About'), Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}" />

                    <TextBlock Text="{x:Bind AppName}" />
                    <TextBlock Text="{x:Bind AppVersionText}" />
                    <TextBlock
                        TextWrapping="Wrap"
                        Text="{x:Bind Strings.Get('Settings.SettingsLocation'), Mode=OneWay}" />
                    <TextBlock
                        TextWrapping="Wrap"
                        Opacity="0.8"
                        Text="{x:Bind SettingsFilePath}" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
